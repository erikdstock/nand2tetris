{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Test Chip",
      // execute ./HardwareSimulator.sh CurrentChipFilename.tst
      "command": "${workspaceFolder}/tools/HardwareSimulator.sh ${fileDirname}/${fileBasenameNoExtension}.tst",
      // "windows": {
      //   I don't know how to do this in a windows environment- should be similar to command above w/ different slashes and .bat?
      //   "command": ".\\scripts\\test.cmd"
      // },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}